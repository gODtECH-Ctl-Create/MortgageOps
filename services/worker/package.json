{
  "name": "@mortgageops/worker",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "tsx watch src/main.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/main.js",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "bullmq": "6.3.4",
    "ioredis": "6.0.0"
  },
  "devDependencies": {
    "@types/node": "latest",
    "tsx": "4.20.6",
    "typescript": "7.0.2",
    "vitest": "5.0.0"
  }
}
